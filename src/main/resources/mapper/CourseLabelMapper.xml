<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zyx.mapper.CourseLabelMapper">
    <resultMap id="courseLabel" type="com.zyx.model.CourseLabel">

        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="user_id" jdbcType="INTEGER" property="userId"/>
        <result column="label_name" jdbcType="VARCHAR" property="labelName"/>
        <result column="create_time" jdbcType="BIGINT" property="createTime"/>
        <result column="state" jdbcType="INTEGER" property="state"/>
        <result column="app_type" jdbcType="INTEGER" property="appType"/>
    </resultMap>


    <!--<select id="queryCourseLabel" resultMap="courseLabel">-->
        <!--SELECT * FROM t_course_label-->
    <!--</select>-->

    <update id="updateState" parameterType="com.zyx.model.CourseLabel">
        UPDATE t_course_label SET state=#{state} WHERE id=#{id}
    </update>

    <select id="queryByState" parameterType="com.zyx.model.CourseLabel" resultMap="courseLabel">
        SELECT * FROM t_course_label WHERE state=#{state}
        <if test="appType != null">
            and app_type=#{appType}
        </if>
    </select>


</mapper>